# แผนที่ Logic — `index.html` → `hylme_pipeline`

เอกสารนี้ตอบคำถามเดียว: **กติกาแต่ละข้อใน `index.html` ตอนนี้อยู่ตรงไหนของ pipeline ใหม่**
ทุกบรรทัดอ้างเลขบรรทัดจริงของ `index.html` ที่ตรวจแล้ว ณ 9 ก.ย. 2026 (34,901,443 ไบต์ · 9,974 บรรทัด)

อ่าน 3 ตารางนี้ก่อน:

| ตาราง | ตอบว่า |
|---|---|
| §1–§7 | logic ที่ **ย้ายมาแล้ว** อยู่ไฟล์ไหน ฟังก์ชันไหน |
| §8 | logic ที่ **ยังไม่ได้ย้าย** — และตอนนี้ตัวเลขจะต่างจากเดิมตรงไหน |
| §9 | วิธีพิสูจน์ว่าตัวเลขไม่เปลี่ยน |

> **สรุปความเสี่ยงที่สำคัญที่สุด ก่อนอ่านอย่างอื่น**
> `recomputeDataset()` บรรทัด 6546–6632 มีขั้นตอน **"กระทบยอดกับรายงานขายฮีลมี 2569"** 3 ขั้น
> ที่ปรับ `Lead Value` ของทุกออเดอร์ให้ยอดรายวันตรงกับ `DAY_TARGET` / `DAY_CH_TARGET`
> **pipeline ยังไม่ได้ทำขั้นนี้** ดังนั้นยอดขายที่ pipeline รายงาน = ยอดดิบจากไฟล์ต้นทาง
> ส่วนแดชบอร์ดเดิมรายงาน = ยอดที่เกลี่ยให้ตรงรายงาน Excel แล้ว → **สองตัวนี้ยังไม่เท่ากัน**
> รายละเอียดและทางเลือกอยู่ใน §8.1 — ต้องตัดสินใจข้อนี้ก่อนเทียบตัวเลขใด ๆ

---

## 1 · ฟังก์ชันพื้นฐาน (ตัวเลข · เบอร์โทร · วันที่ · ที่อยู่)

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `rv()` ปัดทศนิยม 2 ตำแหน่ง | 2402 | `util.rv()` | ตรง |
| `withVat()` | 1517 | `util.with_vat()` | ตรง |
| `VAT_RATE = 0.07` | 1516 | `config/rules.json → vat_rate` | ตรง |
| `pkNum()` / `mcmNum()` | 1985 | `util.num()` | ตรง |
| `allocateWholeBaht()` | 4771 | `util.allocate_whole_baht()` | ตรง — ทดสอบ `[1344.99, 1345.01] → [1345, 1345]` |
| `splitWholeUnitPrices()` | 4786 | `util.split_whole_unit_prices()` | ตรง — ทดสอบ `(1345, 3) → [449×1, 448×2]` |
| `mcNorm()` | 2225 | `util.norm()` | ตรง |
| `mcIsNA()` / `mcPick()` | 2227–2228 | `util.is_na()` / `util.pick()` | ตรง |
| `isMaskedText()` | 2276 | `util.is_masked()` | ตรง |
| `normPhoneDigits()` | 2231 | `util.norm_phone_digits()` | ตรง — ทดสอบ `66894499089 → 0894499089` |
| `extractThaiPhone()` | 2238 | `util.extract_thai_phone()` | ตรง — ทดสอบ `"...อยุธยา13000 0814347070" → 0814347070` |
| `cleanPhone()` | 2250 | `util.clean_phone()` | ตรง |
| `cxPhone()` (9 หลักท้าย ใช้จับคู่ยกเลิก) | 3821 | `util.cx_phone()` | ตรง |
| `cxName()` | 3827 | `util.cx_name()` | ตรง |
| `pkDateTime()` (รองรับ พ.ศ.) | 1993 | `util.pk_datetime()` | ตรง — ทดสอบ `23/07/2569 23:08 → 2026-07-23 23:08:00` |
| `mcDateTime()` | 2310 | `util.mc_datetime()` | ตรง |
| `manualCancelDate()` | 3681 | `util.manual_cancel_date()` | ตรง |
| `bizDateStr()` + `BIZ_DAY_START_HOUR` | 6408 / 6407 | `util.biz_date_str()` + `config/rules.json → biz_day_start_hour` | ตรง (ค่ายังเป็น 0) |
| `mcmShiftDateStr()` | 8962 | `util.shift_date_str()` | ตรง (บวก = ถอยหลัง) |
| `THAI_PROVINCES` 77 จังหวัด | 1524 | `util.THAI_PROVINCES` | ตรง เรียงยาว→สั้นเหมือนกัน |
| `findProvinceInText()` | 1530 | `util.find_province_in_text()` | ตรง |
| `findZipInText()` | 1536 | `util.find_zip_in_text()` | ตรง |
| `resolveProvince()` | 1542 | `normalize.resolve_province()` | ตรง |
| `buildAddress()` | 1583 | `normalize.build_address()` | ตรง |
| `buildRecipientName()` | 1574 | `normalize.build_recipient_name()` | ตรง |
| `isMetro()` + `METRO_PROVINCES` | 1554 / 1522 | `normalize.is_metro()` + `config/channels.json → metro_provinces` | ตรง |
| — (ใหม่) | — | `util.customer_key()` | คัดลอกจาก `hylme_engine/02_load_orders.py: norm_phone()` เพื่อ join กับตาราง `customers` เดิมได้ |

---

## 2 · กติกาจัดหมวดหมู่ (สินค้า · ช่องทาง · ทีม · การชำระเงิน)

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `PANCAKE_SKU_NAME` (5 SKU) | 1962 | `config/products.json → sku_to_name` | ตรง |
| `mapPancakeProductName()` | 1969 | `normalize.map_product_name()` | ตรง |
| `PRODUCT_ALIASES` + `RE_S9` / `RE_V9` | 4544 / 4541–4542 | `config/products.json → product_aliases, re_s9, re_v9` | ตรง ลำดับเดิม |
| `shortProductName()` | 4552 | `normalize.short_product_name()` | ตรง — ทดสอบ `"💚 Hylme Daliy Oils Blend 60 Softgel - 1 กระปุก" → Daily Oils Blend` |
| `PRODUCT_ORDER` | 4512 | `config/products.json → product_order` | ตรง |
| `SHIPPING_LABELS` + `isShippingLine()` | 1515 / 4502 | `config/products.json → shipping_labels` + `normalize.is_shipping_line()` | ตรง |
| `PREORDER_SKUS` + `isPreorderItem()` | 4666 / 4716 | `config/products.json → preorder_skus` + `normalize.is_preorder_item()` | ตรง |
| `OLIVE_SKUS` + `OLIVE_1BOTTLE_PRICE = 300` + `isOliveOil()` | 4670–4672 | `config/products.json → olive_*` + `normalize.is_olive_oil()` | ตรง |
| `defaultApproved()` = `false` + `isSkuApproved()` | 4726–4727 | `config/products.json → sku_default_approved` + `normalize.is_sku_approved()` | ตรง — ค่าเริ่มต้นยังเป็น OnHold ทุก SKU · เพิ่ม `sku_approved_override` ให้ไม่ต้องติ๊กเองทุกวัน |
| `promoLabel()` (แจกฟรี/ลดพิเศษ/ราคาปกติ) | 4578 | `normalize.promo_label()` | ตรง |
| `parseChannel()` | 1555 | `normalize.parse_channel()` | ตรง |
| `canonChannelLabel()` | 4402 | `normalize.canon_channel_label()` | ตรง |
| `channelLabel()` | 4413 | `normalize.channel_label()` | ตรง |
| `CRM_CH_LABEL` / `TELESALES_CH_LABEL` | 4391 / 4386 | `config/channels.json → crm_channel_label / telesales_channel_label` | ตรง |
| `PERSON_ALIASES` + `canonPerson()` | 4425 / 4434 | `config/mappings.json → person_aliases` + `normalize.canon_person()` | ตรง |
| `TEAM3` (3 ทีม) + `teamOfLead()` | 4439 / 4456 | `config/channels.json → teams` + `normalize.team_of()` | ตรง |
| `paymentMethod()` (COD/ปลายทาง → COD) | 4593 | `normalize.payment_method()` | ตรง |
| `isMarketplaceOrder()` | 4603 | `normalize.is_marketplace_order()` | ตรง |
| `paymentMethodOf()` | 4611 | `normalize.payment_method_of()` | ตรง |
| `MC_CHANNEL_ALIAS` + `mcChannelName()` | 2285 / 2291 | `config/channels.json → mc_channel_alias` + `normalize.mc_channel_name()` | ตรง |
| `mcLeadIntro()` | 2298 | `normalize.mc_lead_intro()` | ตรง |
| `mapSalesPerson()` + `SALES_PERSON_MAP` | 4712 / 4626 | `normalize.map_sales_person()` + `config/mappings.json` | ตรง (รวม fallback ตัวพิมพ์เล็ก) |
| `mapPancakeRep()` + `PANCAKE_REP_MAP` | 1956 / 1952 | `normalize.map_pancake_rep()` + `config/mappings.json` | ตรง |
| `TELESALES_PEOPLE` | 4655 | `config/mappings.json → telesales_people` | ตรง |
| `MARKETPLACE_RE` | 4663 | `config/channels.json → marketplace_re` | ตรง |

---

## 3 · การอ่านไฟล์ (Loaders)

### 3.1 Pancake

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `isPancakeWorkbook()` | 2029 | `loaders/pancake.detect()` | ตรง |
| `convertPancakeData()` | 2036 | `loaders/pancake.load()` | ตรง |
| forward-fill `รหัสคำสั่งซื้อ` (บรรทัดสินค้า 2+ ไม่มีรหัส) | 2057–2069 | เดียวกัน | ตรง — **ทดสอบแล้ว** ออเดอร์ 2 สินค้าได้ครบ 2 บรรทัด |
| ค่าส่ง = `ยอดสั่งซื้อทั้งหมด − มูลค่าการสั่งซื้อ` | 2085 | เดียวกัน | ตรง — ทดสอบ 1350−1340 = 10 |
| ออเดอร์แจกฟรี (แยก "มีคอลัมน์แต่ = 0" ออกจาก "ไม่มีคอลัมน์") | 2093–2112 | เดียวกัน (`field_of()` คืน `None` เมื่อไม่มีคอลัมน์) | ตรง — **ทดสอบแล้ว** ยอด 0 ไม่ตกไปใช้ราคาเต็ม |
| วันที่ = `วันที่ยืนยันคำสั่งซื้อ` (fallback = วันที่สร้าง) | 2119–2126 | เดียวกัน | ตรง — ทดสอบ สร้าง 22/07 23:50 ยืนยัน 23/07 00:15 → นับวันที่ 23 |
| `pkPersonName()` สลับคอลัมน์ชื่อ↔นามสกุล | 2011 | `loaders/pancake._person_name()` | ตรง — ทดสอบได้ `Vissanurak Pichaisornplang` |
| `pkChannelToIntro()` | 2024 | `loaders/pancake._channel_to_intro()` | ตรง |
| `PANCAKE_CANCEL_RE` | 1950 | `config/statuses.json → pancake_cancel_re` | ตรง — ทดสอบ ออเดอร์ยกเลิกถูกข้าม |
| เกลี่ยยอดสุทธิลงบรรทัดสินค้า (บรรทัดสุดท้ายรับเศษ) | 2158–2178 | เดียวกัน | ตรง — ทดสอบ 625.61 + 589.34 = 1214.95 พอดี |

### 3.2 MyCloud + Marketplace + ทะเบียนยกเลิก

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `mcFindSheet()` / `isMyCloudWorkbook()` | 2280 / 2283 | `loaders/mycloud.find_sheet()` / `detect()` | ตรง |
| `mcRowSku()` (Shop SKU ก่อน MyCloud SKU) | 2332 | `loaders/mycloud._row_sku()` | ตรง |
| `mcGroupOrders()` | 2339 | `loaders/mycloud._group_orders()` | ตรง |
| `mcBuildProdRows()` (ใช้ unit price เป็นน้ำหนัก ไม่ใช่ราคาจริง) | 2373 | `loaders/mycloud._build_items()` | ตรง รวม `Discount = 0` โดยตั้งใจ |
| `convertMyCloudData()` | 2399 | `loaders/mycloud.load()` | **ต่างโดยตั้งใจ** → §8.3 |
| `convertMarketplaceData()` | 2563 | `loaders/mycloud.load()` (รอบเดียวกัน) | **ต่างโดยตั้งใจ** → §8.3 |
| `MC_CANCEL_RE` | 2203 | `config/statuses.json → mc_cancel_re` | ตรง |
| `MC_OK_STATUS` | 2205 | `config/statuses.json → mc_ok_status` | **แก้ในรอบนี้** → §7 |
| `scanCancelWorkbook()` | 3735 | `loaders/mycloud.load()` → `res.cancels` | ตรง (รวมเข้ารอบอ่านเดียวกัน) |
| `manualCancelHeader()` / `parseManualCancelWorkbook()` | 3669 / 3705 | `loaders/cancel_manual.detect()` / `.load()` | ตรง |
| `isManualCancelWorkbook()` | 3698 | `loaders/cancel_manual.detect()` | ตรง |
| ลำดับตรวจชนิดไฟล์ใน `handleFiles()` | 3007+ | `loaders/detect.DETECTORS` | ตรง — รายงานขาย → oldsheet → gosell → cancel → pagestat → mycloud → pancake |

### 3.3 GoSell (ข้อมูลเก่า พ.ย. 2025 – พ.ค. 2026)

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `GOSELL_HEADER_KEYS` (เจอ 5 จาก 7 คอลัมน์ถือว่าใช่) | 1626 | `loaders/gosell.HEADER_KEYS` | ตรง |
| `findGoSellHeader()` (หัวตารางใน 12 แถวแรก ชีตไหนก็ได้) | 1675 | `loaders/gosell._find_header()` | ตรง — ทดสอบหัวตารางอยู่แถว 3 |
| `gsNorm` / `gsNum` (แยก "ไม่มีค่า" ออกจาก 0) | 1626–1640 | `util.norm()` / `gosell._num()` | ตรง |
| `gsFirstNonEmpty()` (ยอดระดับออเดอร์อยู่บรรทัดเดียว) | 1645 | `loaders/gosell._first()` | ตรง |
| `gsDate()` | 1650 | `loaders/gosell._date()` | ตรง |
| `gsPhone()` | 1662 | `loaders/gosell._phone()` | ตรง |
| `gsProductSku()` (เดา SKU จากชื่อสินค้าเมื่อไฟล์ไม่มีรหัส) | 1666 | `loaders/gosell._guess_sku()` | ตรง — **บันทึกใน `source_audit` ว่าเดามาจากชื่อไหน** |
| `gsIsShipping()` (บรรทัดค่าส่งไม่นับเป็นสินค้า) | 1672 | `loaders/gosell._is_shipping()` | ตรง |
| `convertGoSellData()` | 1696 | `loaders/gosell.load()` | ตรง |
| จับกลุ่มด้วย `เลขที่คำสั่งซื้อ` · `Lead ID = 'GS-' + orderNo` | 1710–1740 | เดียวกัน | ตรง — **รูปแบบตรงกับ 10,250 ใบที่อยู่ในฐานแล้ว** |
| ยอดก่อน VAT = `มูลค่าก่อนภาษี` ถ้า > 0 ไม่งั้น `รวมทั้งสิ้น / 1.07` | 1727–1733 | เดียวกัน | ตรง — ทดสอบทั้งสองทาง |
| สถานะยกเลิก → ข้ามทั้งใบ · ส่งคืนสำเร็จ → โหลดเข้ามาแต่นับไว้ | 1717–1722 | เดียวกัน | ตรง |
| เกลี่ยยอดลงบรรทัดสินค้าตามสัดส่วน `ยอดรวมสินค้า` (บรรทัดสุดท้ายรับเศษ) | 1768–1782 | เดียวกัน | ตรง — ทดสอบ 1,195.81 + 486.43 = 1,682.24 พอดี |
| `leadIntro = [channel, payment].join(', ')` | 1740 | เดียวกัน | ตรง — **ยกข้อบกพร่องเดิมมาด้วย** → §8.9 |
| `GoSell Channel` (ใช้จับช่องทางตอนเกลี่ยยอด) | 1756 | `orders.gosell_channel` → `revenue.xl_bucket_of()` | ตรง |

### 3.4 Google Sheet เก่า (มิ.ย. – 23 ก.ค. 2026)

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `THAI_MONTHS` / `parseThaiMonthYear()` (อ่านเดือนจากชื่อไฟล์) | 3160 | `loaders/oldsheet.parse_thai_month_year()` | ตรง — ของเดิมถ้าไม่เจอจะ `prompt()` ถาม ที่นี่ขึ้น error บอกวิธีตั้งชื่อไฟล์ |
| `RECORDER_TO_FULLNAME` / `mapRecorder()` | 3172 | `loaders/oldsheet.RECORDER_TO_FULLNAME` | ตรง — ชื่อที่ไม่รู้จัก (ยย/กอล์ฟ) ใช้ชื่อเดิม |
| `mapOldProduct()` (**ลำดับการตรวจสำคัญ ห้ามสลับ**) | 3178 | `loaders/oldsheet.map_old_product()` | ตรง — olive → daily oil → mag → gluta → gaba |
| `parseOldPromo()` (แตกสินค้า+จำนวนจากคอลัมน์โปรโมชั่น) | 3188 | `loaders/oldsheet.parse_old_promo()` | ตรง — ทดสอบ "Daily Oils Blend 2 กป, Mag 1 กป" |
| `oldChannelToIntro()` | 3204 | `loaders/oldsheet.old_channel_to_intro()` | ตรง |
| `parseDelivery()` (แยกชื่อ/เบอร์/ที่อยู่จากข้อความก้อนเดียว) | 3216 | `loaders/oldsheet.parse_delivery()` | ตรง — **ยกข้อบกพร่องเดิมมาด้วย** → §8.10 |
| `convertOldData()` | 3229 | `loaders/oldsheet.load()` | ตรง |
| `Lead ID = OLD-{ปี}{เดือน}{วัน}-{ลำดับ}` ลำดับนับต่อเนื่อง **ทั้งไฟล์** | 3253 | เดียวกัน | ตรง — ทดสอบข้ามชีตแล้วลำดับเดินต่อ |
| เวลาเทียม `hh = min(23, 8 + idx/60)` · `mm = idx % 60` (รีเซ็ตทุกวัน) | 3254 | เดียวกัน | ตรง — บันทึกใน audit ว่าเป็นเวลาที่สร้างขึ้น |
| ยอดติดลบ → ข้าม · ยอด 0 บาท → นับเป็นออเดอร์จริง | 3246–3250 | เดียวกัน | ตรง — **ทดสอบแล้วทั้งสองเคส** |
| `leadValuePre = ยอดขาย Sale / 1.07` | 3256 | เดียวกัน | ตรง |
| เกลี่ยยอดลงบรรทัดสินค้าตามสัดส่วน `จำนวน` (บรรทัดสุดท้ายรับเศษ) | 3274–3287 | เดียวกัน | ตรง — ทดสอบ 925.24 + 925.23 = 1,850.47 พอดี |
| `dropOldSheetDays()` (กันซ้ำด้วย "วันที่" ไม่ใช่ Lead ID) | 3298 | `LoadResult.replace_scope` → `db.drop_orders_by_scope()` | ตรง — ลบเฉพาะ `source='Google Sheet (เก่า)'` ของวันที่ไฟล์ใหม่ครอบคลุม |
| `handleOldFiles()` (ชีตชื่อ 1–31 = วันของเดือน) | 3327 | `loaders/oldsheet._day_sheets()` / `detect()` | ตรง |

### 3.5 ★ ไฟล์ "gosell Orders ..." ที่ทีมมี ไม่ใช่ export ดิบจาก GoSell

เปิดไฟล์จริงแล้วเป็นชีต `Leads` / `Leads-Products` = รูปแบบ ReadyPlanet ที่
`convertGoSellData()` แปลงออกมา แล้ว `bakeAndDownload()` เขียนลงไฟล์
`loaders/gosell.py` (§3.3) จึงยังไม่ได้ถูกใช้ — เก็บไว้รองรับถ้าวันหนึ่งได้ไฟล์ดิบจาก GoSell จริง
ไฟล์ที่มีอ่านด้วย `loaders/leads_export.py` แทน

**โชคดีที่ไฟล์นี้มีครบทั้งสองชั้นอยู่แล้ว** เพราะแดชบอร์ดเก็บยอดก่อนเกลี่ยไว้ในคอลัมน์ `__`

| ในไฟล์ | index.html | หมายถึง | pipeline เอาไปเป็น |
|---|---:|---|---|
| `__origValue` | 6551 | ยอดก่อนเกลี่ย (ก่อน VAT) | **ยอดดิบ** → `orders.amount_ex_vat` |
| `Lead Value` | 6552, 6569 | ยอดหลังเกลี่ย = ที่แดชบอร์ดโชว์ | เก็บใน audit ไว้เทียบเท่านั้น |
| `__origVal` / `__origUnit` | 6626 | ยอด/ราคาต่อชิ้นก่อนเกลี่ยของบรรทัดสินค้า | `order_items` |
| `__xlBucket` | 6578 | ช่องทางที่ใช้ตอนเกลี่ย | เก็บใน audit ไว้เทียบ |
| `__cancelled` / `__cancelHow` | 3937 | ผลจับคู่ทะเบียนยกเลิก | เก็บใน audit ไว้เทียบ |
| `__cxPhone` | 3868 | เบอร์ที่แดชบอร์ดล้างแล้ว | ใช้เป็นเบอร์ตั้งต้น |

กฎ: **ยอดดิบ = `__origValue` ถ้าไม่มีค่า ใช้ `Lead Value`** (ไม่มี = ใบนั้นไม่เคยถูกเกลี่ย)

พิสูจน์แล้ว — ไฟล์สองไฟล์ที่มีทับกันช่วง เม.ย. 1,388 ใบ ไฟล์เก่ามี `__origValue`
ไฟล์ใหม่ไม่มี **ยอดดิบตรงกัน 1,387 จาก 1,388 ใบ** (อีก 1 ใบต่างกัน 28 บาท
เพราะมีการแก้ที่ GoSell ระหว่าง export สองครั้ง)

> ไฟล์ที่ export ตอนเปิดโหมดปิดบัง PDPA จะได้ชื่อ/ที่อยู่เป็น `น******ง` และเบอร์ว่าง
> loader ตัดค่าที่ถูกปิดบังทิ้ง ไม่เก็บดาวลงฐาน — ใบเหล่านั้นจับคู่ลูกค้าซ้ำไม่ได้
> (ไฟล์ 1-11-25 มี 2,616 ใบ · ไฟล์ 2026-06-22 มี 700 ใบ) เป็นข้อจำกัดของไฟล์ ไม่ใช่ของ pipeline

### 3.6 ที่มาของยอดดิบ (ของใหม่ ไม่มีในของเดิม)

ทุก loader ที่โหลด "ยอดดิบ" เขียนแถวลง `source_audit` หนึ่งแถวต่อออเดอร์ต่อไฟล์
เก็บ ไฟล์ · ชีต · เลขแถวตามที่เห็นใน Excel · คีย์ · ยอดที่ไฟล์เขียนไว้ · คอลัมน์ที่ใช้ ·
และ JSON ของทุกการ mapping (ช่องทางดิบ → ช่องทางที่ใช้ · SKU ที่เดามา · รายการที่แตกจากโปรโมชั่น)

ดูได้ด้วย `python revenue_report.py <order_id>` — จะพิมพ์ทั้งที่มาและทุกชั้นที่ปรับยอด

---

## 4 · การกระทบยอดออเดอร์ยกเลิก

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `cancelLeadIndexes()` | 3853 | `reconcile._Index` | ตรง + เพิ่มดัชนีจาก `order_sources` |
| `cancelChanToken()` / `cancelLeadChanToken()` | 3833 / 3841 | `reconcile._chan_token()` | ตรง |
| `narrow()` (ยอดตรง → วันตรง → วันก่อนหน้า 1 วัน) | 3886–3895 | `reconcile._narrow()` | ตรง |
| บันไดจับคู่ 6 ชั้น L1–L6 | 3897–3925 | `reconcile._match_one()` | ตรง ลำดับเดิม |
| กันหักซ้ำด้วย `free()` | 3884 | `reconcile` — `taken` set + คีย์ที่บันทึกไว้แล้ว | ตรง |
| `ambiguous` เมื่อ L6 มีผู้สมัคร > 1 | 3922 | เดียวกัน | ตรง |
| ไฟล์ยกเลิกของ Hylme เขียนทับ `Lead Value` | 3928–3932 | `reconcile.run()` + `config/rules.json → manual_cancel_overrides_amount` | ตรง + **เพิ่มการเตือนเมื่อยอดสองฝั่งไม่ตรง** |
| `registerCancels()` (ไฟล์ทีหลังทับไฟล์ก่อน) | 3785 | `reconcile.upsert_cancellations()` | ตรง |
| — (ใหม่) | — | ตาราง `cancellations` เก็บผลการจับคู่ถาวร | **ต่างจากเดิม** → §8.5 |

---

## 5 · การสร้างไฟล์ MyCloud

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| `mapSalesChannel()` (Lead Intro ก่อน → ผู้ดูแล) | 4734 | `exports/mycloud_export.map_sales_channel()` | ตรง |
| `CH` รหัสช่องทาง 3037–3044 | 4646 | `config/channels.json → mc_sales_channel_codes` | ตรง |
| `TELESALES_MC_PERSON` | 4665 | `config/mappings.json → telesales_mc_person` | ตรง |
| `buildMcRows()` | 4795 | `exports/mycloud_export.build_rows()` | ตรง |
| ข้ามออเดอร์ที่ `Source` = MyCloud | 4805 | เดียวกัน | ตรง |
| ข้ามออเดอร์ที่ไม่รู้วิธีชำระเงิน | 4807–4812 | เดียวกัน + **fallback ไปคอลัมน์ `payment_method`** สำหรับออเดอร์เก่าที่ไม่มี Lead Intro | ต่างเล็กน้อย → §8.6 |
| ข้ามออเดอร์ที่ไม่มี SKU | 4816–4820 | เดียวกัน | ตรง |
| หมายเหตุ 5 แบบ (ชื่อ/เบอร์/ที่อยู่/รหัสไปรษณีย์/จังหวัด) | 4830–4836 | เดียวกัน | ตรง |
| `deliveryMode` = (COD/โอน) × (ปริมณฑล/ต่างจังหวัด) | 4838–4840 | `normalize.delivery_mode()` | ตรง |
| สถานะ = Fulfill ก็ต่อเมื่ออนุมัติครบทุก SKU | 4845 | เดียวกัน | ตรง |
| หมายเหตุ Pre-order | 4846–4851 | เดียวกัน | ตรง — ทดสอบขึ้น `Pre-order: Organic Extra Virgin Olive Oil` |
| Olive Oil 1 ขวด และเป็นสินค้าเดียว → 300 | 4874 | เดียวกัน | ตรง — **ทดสอบแล้ว** ได้ 300 ไม่ใช่ 310 |
| ยุบ SKU ซ้ำในออเดอร์เดียวให้เหลือแถวเดียว | 4884–4896 | เดียวกัน | ตรง |
| เฉพาะบรรทัดแรกมีชื่อ/เบอร์/ที่อยู่/ขนส่ง/สถานะ | 4900–4906 | เดียวกัน | ตรง — ทดสอบไฟล์ออกมา 2 บรรทัด รวม 669+631 = 1300 ตรงยอด COD |
| `MC_FIELD_COL` (A/C/E/G/I/K/L/N/R/U/V/W/X) · แถวเริ่ม 3 | 4921 / 5182 | `exports/mycloud_export.FIELD_COL` / `START_ROW` | ตรง |
| `MC_TEMPLATE_XLSX_BASE64` (176 KB ในโค้ด) | 4915 | `config/templates/mycloud_template.xlsx` | ตรง ไบต์เดียวกัน |
| `mcZipRead/mcZipWrite/mcCrc32/mcInflateRaw/mcDeflateRaw/mcSetCell/mcFillTemplate` (~300 บรรทัด) | 4937–5236 | `openpyxl` 3 บรรทัด | แทนที่ — dropdown ของ template ยังอยู่ครบ 6 ตัว (ตรวจแล้ว) |

---

## 6 · การรวมข้อมูล (แทน `recomputeDataset`)

| index.html | บรรทัด | pipeline | สถานะ |
|---|---:|---|---|
| merge leads ตาม `Lead ID` ไฟล์ทีหลังชนะ | 6457–6462 | `db.upsert_order()` — UPSERT บน `order_id` | ตรง |
| ตัด `Lead Name = 'Deleted'` | 6467 | `loaders/pancake` — เปลี่ยนชื่อเป็น `ออเดอร์ <id>` ตามโค้ดเดิมบรรทัด 2143 | ตรง |
| `RETURNED_RE` ตัดออเดอร์ส่งคืนสำเร็จ | 6474 | `config/statuses.json → returned_re` | **ยังไม่ได้ใช้** → §8.4 |
| dedupe บรรทัดสินค้า (composite key + ลำดับซ้ำในไฟล์เดียวกัน) | 6491–6503 | `db.replace_order_items()` — ลบทั้งชุดแล้วเขียนใหม่ | ตรงในผลลัพธ์ (ดู §8.7) |
| ขั้นกระทบยอดกับรายงานขาย 2569 (3 ขั้น) | 6546–6632 | `pipeline/revenue.py` | ย้ายแล้ว → §8.1 |
| `buildPeriod()` + การ์ดฝั่งนำเสนอทั้งหมด | 6761–8500 | `dashboard.html` (อ่านจาก `.json`) | ย้ายแล้ว → **§8.15** |

---

## 7 · สถานะ MyCloud — สิ่งที่แก้ในรอบนี้

ตรวจจากไฟล์จริง `07.xlsx` + `08.xlsx` ทั้ง 3 คอลัมน์ (`Order Status`, `MKP Order Status`, `Shipment Status`)
ได้สถานะทั้งหมด 23 แบบ ดังนี้ (ตัวหนา = ยังไม่อยู่ใน `MC_OK_STATUS` เดิม จึงขึ้นเตือน):

| สถานะ | จำนวน | `MC_CANCEL_RE` จับไหม | ผลต่อยอดขาย |
|---|---:|---|---|
| N/A · Delivered · COMPLETE · Shipped · COMPLETED · Shipping · confirmed · DELIVERED · SHIPPED · READY_TO_SHIP · ready_to_ship · PROCESSED | — | ไม่จับ | นับเป็นยอดขาย · รู้จักอยู่แล้ว |
| **SHIP_TO_DROP_POINT** | 1,075 | ไม่จับ | นับเป็นยอดขาย |
| **WAITING_CONFIRM** | 859 | ไม่จับ | นับเป็นยอดขาย |
| **IN_PROGRESS** | 735 | ไม่จับ | นับเป็นยอดขาย |
| **TO_CONFIRM_RECEIVE** | 54 | ไม่จับ | นับเป็นยอดขาย |
| **IN_TRANSIT** | 8 | ไม่จับ | นับเป็นยอดขาย |
| **Ready to pick / Ready to pack / Ready to ship** | 4 | ไม่จับ | นับเป็นยอดขาย |
| CANCELLED · Canceled · CANCEL | 60 | **จับ** | ไม่นับเป็นยอดขาย |

**ข้อสังเกตที่ต้องบอกตรง ๆ** — ในสองไฟล์นี้ **ไม่พบ `AWAITING_COLLECTION`** เลย
(พบ `IN_TRANSIT` 8 ครั้ง ส่วนที่พบเยอะจริง ๆ คือ `SHIP_TO_DROP_POINT` · `WAITING_CONFIRM` · `IN_PROGRESS`)
เป็นไปได้ว่า `AWAITING_COLLECTION` มาจากไฟล์เดือนอื่นหรือช่องทางอื่น

**สิ่งที่ทำ** — เพิ่มทั้ง 3 สถานะที่ระบุมา **บวกกับอีก 6 สถานะที่พบจริงในไฟล์** เข้า `mc_ok_status`
ทั้งหมดนี้ไม่เข้า `MC_CANCEL_RE` อยู่แล้ว จึงถูกนับเป็นยอดขายทั้งก่อนและหลังแก้
**การแก้นี้เปลี่ยนแค่ข้อความเตือน ไม่เปลี่ยนตัวเลขแม้แต่บาทเดียว**

`CANCELLED` / `Canceled` / `CANCEL` **ไม่ใส่** เข้า `mc_ok_status` — เพราะเป็นสถานะยกเลิกจริง
(และโค้ดเดิมไม่เคยเตือนสถานะกลุ่มนี้อยู่แล้ว เพราะ `return` ออกไปก่อนถึงบรรทัดที่เช็ค — บรรทัด 2436 vs 2437)

---

## 8 · สิ่งที่ยังไม่ได้ย้าย และผลต่อตัวเลข

### 8.1 ★ กระทบยอดกับรายงานขายฮีลมี 2569 — เรื่องใหญ่ที่สุด

`recomputeDataset()` บรรทัด 6546–6632 ปรับ `Lead Value` ของทุกออเดอร์ 3 ขั้น:

| ขั้น | บรรทัด | ทำอะไร |
|---|---:|---|
| 1 | 6552–6555 | คืนค่าดิบ (`__origValue`) ก่อนคำนวณใหม่ทุกครั้ง |
| 2 | 6558–6575 | ออเดอร์ marketplace ที่เดือน ≤ `MKP_FIX_UNTIL` (2026-05) เขียนทับด้วยยอดจริงจาก `MKP_TRUTH` (1,275 รายการ) หรือ `MKP_FACTOR` (ตัวคูณรายเดือน) แล้ว **ล็อก** ไม่ให้ขั้น 3 แตะ |
| 3 | 6577–6622 | ออเดอร์ที่เหลือของแต่ละวัน **ย่อ/ขยายตามสัดส่วน** ให้ยอดรายวัน × ช่องทาง เท่ากับ `DAY_TARGET` / `DAY_CH_TARGET` ซึ่งคัดมาจากไฟล์ Excel "รายงานขายฮีลมี 2569" |
| 3.4 | 6626–6632 | ปรับยอดรายสินค้าด้วยอัตราส่วนเดียวกัน เพื่อให้การ์ดสินค้าไม่เพี้ยน |

**ผลตอนนี้** — ยอดขายที่ pipeline รายงาน = ยอดดิบจาก Pancake/MyCloud
ยอดขายที่แดชบอร์ดเดิมรายงาน = ยอดที่เกลี่ยให้ตรงรายงาน Excel แล้ว → **ยังไม่เท่ากันแน่นอน**

**ต้องเลือกก่อนเทียบตัวเลข**

| ทางเลือก | ได้อะไร | เสียอะไร |
|---|---|---|
| **ก. ย้ายมาทั้งขั้น 2+3** | ตัวเลขตรงกับแดชบอร์ดเดิมและรายงาน Excel เป๊ะ | ต้องย้าย `DAY_TARGET` (253 วัน) + `DAY_CH_TARGET` เข้าฐานข้อมูล และต้องอัปเดตทุกเดือนเมื่อรายงาน Excel ออก |
| **ข. ย้ายเฉพาะขั้น 2** | ยอด marketplace ตรง (ใช้ `revenue_truth` ที่มีตารางรออยู่แล้ว) | ยอดรวมรายวันยังไม่ตรงรายงาน Excel |
| **ค. ไม่ย้ายเลย** | ยอดคือของจริงจากระบบต้นทาง ไม่ถูกเกลี่ย | ตัวเลขต่างจากที่ทีมใช้อยู่ ต้องอธิบายทุกครั้ง |

ผมไม่ตัดสินใจให้ เพราะบันทึกไว้ว่า **"ยึดรายงานขายฮีลมี 2569 เป็นยอดที่ถูกต้อง เมื่อขัดกับตัวเลขจากระบบอื่น"**
ซึ่งชี้ไปทาง (ก) แต่ (ก) หมายความว่าต้องมีขั้นตอนนำเข้ารายงาน Excel รายเดือนเพิ่มเข้ามาใน pipeline ด้วย

### 8.2 Conversion Rate — ยังไม่ได้ย้าย (กำลังทำในรอบนี้)

โครงสร้างจริงใน `index.html` มี **ตัวเลข 2 ชุด** ที่ใช้ตัวตั้งคนละอย่าง — ต้องทำให้ครบทั้งคู่:

| ตัวเลข | สูตร | ตัวตั้งมาจาก | ใช้ที่ |
|---|---|---|---|
| **หลัก (รายเพจ)** | `closes ÷ newCust` | **ออเดอร์ในไฟล์ leads** ที่ `canonLeadPage()` แมปเข้าเพจนั้น (บรรทัด 8232) | ตารางรายเพจ |
| รายวัน / รวมหลายเพจ | `psConv(m) = m.orders ÷ m.newCust` | คอลัมน์ `ยอดออเดอร์` **ในไฟล์ page-stat เอง** (บรรทัด 4328) | ตารางรายวัน + แถว "รวมหลายเพจ" |

**กติกาสำคัญที่ห้ามพลาด** (บรรทัด 8219–8228)
ตัวตั้ง `closes` นับเฉพาะวันที่ไฟล์ page-stat **มีข้อมูลจริง** (`ps.dates`) ไม่ใช่ทั้งช่วงที่เลือก
เพราะตัวหาร (`ลูกค้าใหม่`) มีแค่วันที่มีไฟล์ — ถ้าตัวตั้งเป็นทั้งเดือน Conversion จะพุ่งเกินจริง

รายละเอียดอื่นที่ต้องยกมาให้ครบ:

| index.html | บรรทัด | จะไปอยู่ที่ |
|---|---:|---|
| `PS_COLS` (10 คอลัมน์) | 4181 | `loaders/pagestat.PS_COLS` |
| `psDate()` (d/m/yyyy + พ.ศ.) | 4197 | `loaders/pagestat._ps_date()` |
| `psPageKey()` (ชื่อเพจ → key) | 4207 | `loaders/pagestat.page_key()` |
| `psHeader()` (หัวตารางไม่อยู่บรรทัดแรก · แยก by_time / by_page) | 4217 | `loaders/pagestat._find_header()` |
| `isPageStatWorkbook()` (ต้องมีคำว่า "สถิติหน้า" ใน A1) | 4228 | `loaders/pagestat.detect()` |
| `parsePageStatWorkbook()` | 4236 | `loaders/pagestat.load()` |
| `collectPageStat()` (กันนับซ้ำด้วยคีย์ `วัน\|เพจ` · จัดลำดับไฟล์เพจเดียวก่อน) | 4289 | `exports/dashboard._collect_page_stat()` |
| `canonLeadPage()` | 4373 | `normalize.lead_page_key()` |
| `PAGE_DEFS` (7 เพจ) | 4331 | `config/channels.json → page_defs` |

### 8.3 ออเดอร์ marketplace ได้ `MKP-` อย่างเดียว (ต่างโดยตั้งใจ)

เดิม: ไฟล์ MyCloud หนึ่งไฟล์ถูกอ่าน 2 รอบ (`convertMyCloudData` + `convertMarketplaceData`)
ออเดอร์ marketplace ใบเดียวจึงอาจเกิดเป็นทั้ง `MC-xxx` และ `MKP-xxx` แล้วหักล้างกันด้วยการเทียบยอดเงิน
(`mkpTakeAmountMatch` บรรทัด 2553 + `MKP_AMOUNT_EPSILON = 0.02`)

ใหม่: อ่านรอบเดียว ตัดสินตั้งแต่ต้นว่าช่องทาง marketplace → `MKP-` เท่านั้น
**เจตนาให้ผลเท่าเดิมแต่ไม่พึ่งความบังเอิญของยอดเงิน — ต้องเทียบจำนวนออเดอร์และยอดรวมให้เห็นก่อน**

อีกจุด: ออเดอร์ที่ `Shop Order No` ขึ้นต้นด้วย `PK-` เดิม**ข้ามทิ้ง** (บรรทัด 2432)
ใหม่ผูกข้อมูลจัดส่ง (`mc_order_no`, `tracking_no`, `fulfilment_events`) กลับเข้าออเดอร์ Pancake เดิม
ไม่สร้างออเดอร์ใหม่ จึงไม่กระทบยอดขาย — ไฟล์เดือน 07 ผูกได้ 178 ใบ

### 8.4 `RETURNED_RE` ยังไม่ได้ใช้

บรรทัด 6474–6477 ตัดออเดอร์ "ส่งคืนสำเร็จ" ออกจากยอดขาย (คอมเมนต์ระบุว่าเคยทำยอดสูงเกินจริง ม.ค. 21,043 / เม.ย. 30,593 / พ.ค. 31,916 บาท)
ค่านี้อยู่ใน `config/statuses.json` แล้ว แต่ยังไม่มีจุดเรียกใช้ เพราะกฎนี้ใช้กับคอลัมน์ `GoSell Order Status`
ซึ่งมาจาก **loader ของ GoSell ที่ยังไม่ได้ย้าย** (§8.8) — จะทำพร้อมกัน

### 8.5 ผลการจับคู่ยกเลิกถูกบันทึกถาวร (ต่างโดยตั้งใจ)

เดิมคำนวณใหม่ทุกครั้งที่ recompute → เคสที่เคยชัดเจนกลายเป็น ambiguous ได้ในวันถัดมา
ใหม่บันทึกลงตาราง `cancellations` แล้วใช้ต่อ (`--recheck-cancels` เพื่อคำนวณใหม่ทั้งหมด)
ผลรอบแรกเท่าเดิม ต่างกันเฉพาะรอบถัด ๆ ไป — และเป็นการต่างที่ทำให้ **เสถียรขึ้น** ไม่ใช่เพี้ยน

### 8.6 วิธีชำระเงินของออเดอร์เก่า

`buildMcRows` เดิมข้ามออเดอร์ที่ไม่มี COD/โอน ใน Lead Intro
ออเดอร์ที่โหลดเข้าฐานก่อนมี pipeline ไม่มีคอลัมน์ `lead_intro` เลย จึงถูกข้ามทั้งหมด (105 ใบในวันทดสอบ)
จึงเพิ่ม fallback ไปอ่านคอลัมน์ `payment_method` ที่ `02_load_orders.py` คำนวณไว้แล้ว
**ใช้เฉพาะกับออเดอร์เก่า** ออเดอร์ใหม่ที่ผ่าน pipeline มี Lead Intro ครบ จึงเข้าเส้นทางเดิมทุกใบ

### 8.7 dedupe บรรทัดสินค้า

เดิมใช้ composite key + ตัวนับลำดับซ้ำภายในไฟล์เดียวกัน (บรรทัด 6491–6503)
ใหม่ลบบรรทัดสินค้าของออเดอร์นั้นทั้งชุดแล้วเขียนใหม่จากไฟล์ล่าสุด
ผลลัพธ์เหมือนกันเมื่อไฟล์ล่าสุดสมบูรณ์ — **ต่างกันถ้าออเดอร์เดียวมีสินค้ากระจายอยู่หลายไฟล์**
ยังไม่เจอเคสแบบนั้นในข้อมูลจริง แต่บันทึกไว้เป็นข้อสมมติที่ต้องยืนยัน

### 8.8 ยังไม่ได้ย้ายเลย

| index.html | บรรทัด | หมายเหตุ |
|---|---:|---|
| `parseConvStatWorkbook()` Conversion รายคน | 2851 | ไฟล์ "สถิติการเข้าร่วม" คนละไฟล์กับ page-stat |
| `parseChatWorkbook()` รายงานแชท | 4465 | |
| `parseMcMonthlyWorkbook()` สรุปรายเดือน | 9006 | |
| `buildPosRows()` / `exportPosTransaction()` | 5802 / 5886 | |
| `buildPointRows()` / `exportPointTemplate()` แต้มสมาชิก | 5976 / 6051 | |
| `buildContactRows()` / `exportContactTemplate()` | 5304 / 5402 | |
| `clBuildRows()` / `exportCallsheet()` ชีตโทร | 5487 / 5574 | |
| `exportSegmentation()` | 9505 | |
| `exportRxPos/Orders/Both/DailyChannel()` | 6177–6207 | |
| `TARGETS` / `loadTargets()` เป้าหมาย | 2996 | |

> `convertGoSellData()` (1696) และ `convertOldData()` (3229) ย้ายแล้วในรอบนี้ → §3.3 · §3.4

### 8.9 GoSell: ช่องทางที่แสดงในกราฟเป็น "ไม่ระบุช่องทาง" (ยกของเดิมมาทั้งดุ้น)

`convertGoSellData` สร้าง `Lead Intro` เป็น `"Facebook, โอน"` — **ไม่มีเครื่องหมาย `:`**
แต่ `parseChannel` (2xxx) จับ Facebook/Instagram ด้วย regex ที่ **บังคับว่าต้องมี `:`**
`/(Facebook|Instagram)\s*:\s*([^\n,]+)/i` ออเดอร์ GoSell ที่ช่องทางเป็น Facebook จึงตกไปเป็น
"ไม่ระบุช่องทาง" ทั้งหมด (Shopee / Lazada / Tiktok / LINE ไม่มีปัญหา เพราะจับด้วยคำล้วน)

* **ยอดขายไม่กระทบ** — การเกลี่ยยอดใช้ `gosell_channel` (คอลัมน์ `GoSell Channel`) ผ่าน `XL_GS_MAP`
  ไม่ได้ใช้ `parseChannel` เพราะฉะนั้นยอดรายช่องทางในรายงานยังถูก
* **กระทบเฉพาะกราฟแยกช่องทางบนแดชบอร์ด** ของช่วง พ.ย. 2025 – พ.ค. 2026
* ตอนนี้ **คงพฤติกรรมเดิมไว้** ตามหลัก "reproduce index.html ให้ได้ก่อน"
  ถ้าจะแก้ ควรแก้ที่ `gosell.load()` ให้ประกอบ intro เป็น `"Facebook : <ชื่อช่องทางการขาย>"`
  แล้วรันเทียบยอดรายช่องทางก่อน–หลัง

### 8.10 เบอร์โทรที่มี "66" อยู่กลางเบอร์ ถูกแกะจากข้อความไม่เจอ (ยกของเดิมมาทั้งดุ้น)

`extractThaiPhone` (2240) ตัดรหัสประเทศด้วย `/(?:\+|00)?66[\s\-]?(?=[689]\d)/g` ซึ่ง
**ไม่ได้บังคับว่า `66` ต้องอยู่หัวเบอร์** เบอร์อย่าง `0866666666` จึงโดนแทน `66` ตรงกลางด้วย `0`
เหลือ 7 หลัก แล้วหาเบอร์ไม่เจอ ออเดอร์นั้นกลายเป็น "ไม่มีเบอร์"

* **ยอดขายไม่กระทบ** — เบอร์ไม่ได้ใช้คิดเงิน
* **กระทบการจับคู่ลูกค้า** — ใบนั้นถูกนับเป็นลูกค้าใหม่ ทั้งที่เป็นลูกค้าเดิม
* วัดกับฐานจริงแล้ว: เบอร์ลูกค้าไม่ซ้ำ 9,908 เบอร์ มี **135 เบอร์ (1.36%)** ที่เข้าเงื่อนไขนี้
* กระทบเฉพาะเบอร์ที่ต้อง "แกะจากข้อความ" — ก็คือ Google Sheet เก่า (ช่อง `ข้อมูลการจัดส่ง`)
  และช่องทางสำรองของ GoSell ไฟล์ที่มีคอลัมน์เบอร์แยกไม่ผ่านทางนี้
* ค่าเริ่มต้นคง **กติกาเดิม** ไว้ เปิดตัวแก้ได้ที่ `config/rules.json → phone_country_code_strict: true`

### 8.11 เพดานการเกลี่ยยอด (ของใหม่ ไม่มีในของเดิม)

`config/revenue_rules.json → calibration_max_ratio` — **ตอนนี้ตั้งไว้ที่ `0` = ปิด**
เพื่อให้ยอด ม.ค.–มิ.ย. ตรงรายงาน Excel เป๊ะตามหลัก "reproduce ระบบเดิมให้ได้ก่อน" (§8.12)
ตั้งกลับเป็น `5.0` เมื่อไรก็ได้ ถ้าจะโหลดไฟล์ย้อนหลังทีละบางส่วน

ตอนที่แดชบอร์ดเดิมเกลี่ยยอด มันมีข้อมูล **ครบทั้งวัน** อัตราการเกลี่ยจึงใกล้ 1 เสมอ
แต่ pipeline โหลดไฟล์ทีละไฟล์ ถ้าวันไหนยอดดิบยังมาไม่ครบ ออเดอร์ไม่กี่ใบที่มีจะถูกดันให้
พองขึ้นหลายเท่าเพื่อให้ยอดรวมเท่าเป้า — ยอดผิดแบบเงียบ ๆ

เกินเพดานเมื่อไร ช่องทางนั้นของวันนั้น **ไม่ถูกเกลี่ย** ยอดที่แสดง = ยอดดิบ และขึ้นเตือนในรายงาน
พร้อมบอกว่าดิบเท่าไร เป้าเท่าไร กี่ใบ ใส่ `0` เพื่อปิดเพดาน (กลับไปเกลี่ยเสมอเหมือนของเดิม)

**ข้อควรระวังในการใช้งาน: โหลดไฟล์ต้นทางให้ครบทั้งเดือนก่อนค่อยดูยอด** ไฟล์ครึ่งเดือนทำให้
ยอดที่แสดงของวันที่ยังไม่ครบเพี้ยน (ถ้าไม่มีเพดาน) หรือไม่ถูกเกลี่ย (ถ้ามีเพดาน)

### 8.12 ★ ผลการพิสูจน์กับไฟล์จริงครบชุด (10 ก.ย. 2026)

โหลดครบ: GoSell export 2 ไฟล์ · Google Sheet มิ.ย.+ก.ค. · รายงานขาย Excel ·
Pancake + marketplace + สถิติหน้า ของวันปัจจุบัน → 19,696 ออเดอร์ ใช้เวลา 110 วินาที

**ปิดเพดาน (`calibration_max_ratio: 0` — ค่าที่ใช้อยู่ตอนนี้) — ยอดที่แสดงตรงรายงาน Excel ทุกเดือน**

| เดือน | ยอดที่ระบบใหม่คำนวณ | รายงาน Excel | ต่าง |
|---|---:|---:|---:|
| 2026-01 | 1,422,340.79 | 1,422,340.38 | +0.41 |
| 2026-02 | 1,270,550.25 | 1,270,550.55 | −0.30 |
| 2026-03 | 1,380,516.34 | 1,380,516.57 | −0.23 |
| 2026-04 | 1,271,492.70 | 1,271,492.70 | +0.00 |
| 2026-05 | 1,112,859.35 | 1,112,859.02 | +0.33 |
| 2026-06 | 1,964,996.39 | 1,964,996.70 | −0.31 |

ต่างกันไม่เกิน 0.41 บาท = เศษการปัดทศนิยมล้วน ๆ **ยอดดิบไม่ถูกแตะ** ทุกการปรับอยู่ใน
`revenue_adjustment` 7,397 แถว และตามกลับไปถึงแถวในไฟล์ต้นทางได้ทุกใบผ่าน `source_audit`

**ถ้าเปิดเพดานที่ 5.0** — ต่างจากรายงาน 3 เดือน และต่างเท่ากับ 3 กลุ่มที่ถูกกันไว้พอดี

| วัน × ช่องทาง | ยอดดิบ | เป้า | อัตรา | ผลต่อเดือน |
|---|---:|---:|---:|---:|
| 2026-03-19 Lazada | 22.96 | 853.36 | ×37.2 | มี.ค. −831 |
| 2026-04-12 Lazada | 38.00 | 1,644.30 | ×43.3 | เม.ย. −1,606 |
| 2026-06-28 Other | 37,589.97 | 2,760.63 | ×0.073 | มิ.ย. +34,829 |

สองรายการแรก = วันนั้นมีออเดอร์ Lazada ใบเดียวยอดหลักสิบ แต่รายงานบอกหลักร้อย/หลักพัน
รายการที่สาม = ออเดอร์ Google Sheet 28 ใบที่ **คอลัมน์ "ช่องทางสั่งซื้อ" ว่าง** จึงตกไปอยู่ Other
(ทั้งไฟล์ มิ.ย.+ก.ค. มี 190 แถวที่ช่องทางว่าง — เป็นเรื่องคุณภาพข้อมูลต้นทาง ไม่ใช่โค้ด)

ตั้งแต่ ก.ค. เป็นต้นไปไม่มีรายงาน Excel จึงไม่มีการเกลี่ยเลย เพดานนี้ไม่มีผลกับข้อมูลใหม่

### 8.14 วงจรของปัญหาที่ตรวจเจอ (ของใหม่ ไม่มีในของเดิม)

ของเดิมเตือนบนหน้าจอแล้วหายไปพร้อมการรีเฟรช ไม่มีความจำว่าอันไหนเคยดูแล้ว
pipeline รอบแรกดีขึ้นตรงที่บันทึกลง `validation_log` แต่ยังเป็น append-only ล้วน ๆ
`resolved_at` มีคอลัมน์แต่ไม่เคยถูกเขียน จึงเตือนซ้ำทุกวันเหมือนเดิม

**ตารางใหม่ `validation_issue`** — หนึ่งแถวต่อ (กฎ, สิ่งที่ถูกตรวจ) ไม่โตตามจำนวนรอบ
`validation_log` ยังทำหน้าที่เดิมทุกอย่าง ไม่มีการลบ (เพิ่มคอลัมน์ `scope` อย่างเดียว)

สองแกนที่แยกกันเด็ดขาด

| แกน | ใครตัดสิน | ค่า |
|---|---|---|
| `scope` | ระบบ คำนวณใหม่ทุกรอบ | `historical` (ต้นทางปิดแล้ว) · `current` (ต้นทางยังอยู่) |
| `status` | คน ระบบไม่เขียนทับ | `NEW` · `REVIEWED` · `IGNORED` · `RESOLVED` |

**เกณฑ์ตัดสิน `scope` — ใช้ `orders.source` เทียบ `config/validation.json → live_sources`**

| ตัวเลือกที่พิจารณา | ใช้ได้ไหม | เหตุผล (วัดจากฐานจริง) |
|---|---|---|
| prefix ของ `order_id` | ไม่ได้ | จับได้แค่ `GS-` กับ `L0000...` ยังเหลือ source เก่าอีก 10 แบบที่ไม่มี prefix ร่วมกัน |
| `orders.data_state` | **กลับด้าน** | GoSell = `raw` 10,947 ใบ · Pancake = `pre_reconciled` 3,189 ใบ |
| `orders.source` | **ใช้** | ตรงกับ "ระบบไหนยังป้อนข้อมูลอยู่" — GoSell เงียบมา 82 วัน · Pancake ล่าสุดวันนี้ |

สิ่งที่ตรวจแล้วไม่ใช่ออเดอร์ (เดือนในรายงานขาย · คีย์ทะเบียนยกเลิก) ใช้ `historical_until`
เทียบวันที่แทน — ออเดอร์ไม่ใช้วันที่เพราะ Pancake เริ่ม 23 ก.ค. ทับกับ Google Sheet ที่จบ 31 ก.ค.

**ไม่มีกฎข้อไหนถูกปิด** ทุกกฎยังตรวจครบเหมือนเดิม `calibration_large_ratio` ก็ไม่ต้องมี
เคสพิเศษ — 1,927 รายการเป็น GoSell/Google Sheet ทั้งหมดจึงเป็น audit-only เอง
ถ้าวันหน้า Pancake โดนเกลี่ยเกิน 20% จะขึ้นเตือนทันที

ผลกับฐานจริง: จาก **3,144 รายการ เหลือ 166 รายการที่ต้องลงมือ (5.3%)**

### 8.13 คุณภาพข้อมูลต้นทางที่เจอตอนโหลดไฟล์จริง

เรื่องเหล่านี้แก้ที่ต้นทาง ไม่ได้แก้ที่โค้ด (แก้โค้ดจะกลายเป็นเดาแทนทีม)

| เจออะไร | จำนวน | ผล |
|---|---:|---|
| Google Sheet: "ช่องทางสั่งซื้อ" ว่าง | 190 แถว | ตกไปช่องทาง Other |
| Google Sheet: ชื่อเพจสะกดต่างกัน — "เพจ Hyme ครบจบเรื่องไขมัน" (ขาด l) · "Instragram" · "IG" | 6 แถว | จับคู่ช่องทางไม่ตรง |
| Google Sheet: ชื่อสินค้าไม่ตรงกติกา | 13 บรรทัด | ไม่มี SKU |
| GoSell export: ชื่อ/ที่อยู่/เบอร์ถูกปิดบัง | 3,316 ใบ | จับคู่ลูกค้าซ้ำไม่ได้ |

---

## 8.15 · Phase 5 — หน้าแดชบอร์ด `dashboard.html`

`dashboard.html` แทนการ์ดฝั่ง "นำเสนอ" ของ `index.html` ทั้งหมด
CSS ทั้งก้อนคัดลอกมาจาก `index.html` ตรง ๆ หน้าตาจึงเหมือนเดิม ต่างกันแค่แหล่งข้อมูล

### การ์ดที่ย้ายมาแล้ว

| index.html | บรรทัด | ใช้ไฟล์ไหน | หมายเหตุ |
|---|---:|---|---|
| `renderKPI()` | 7490 | `daily` + `items` | ออเดอร์ = `orders − cancelled_orders` |
| `renderTrend()` | 7507 | `daily` (กรองแล้ว) | กดแท่งเพื่อกระโดดไปวันนั้น |
| `renderProductChart()` | 7555 | `items` | สลับ ชิ้น / ออเดอร์ ได้เหมือนเดิม |
| `renderPromoChart()` | 7589 | `promotions` | `promoLabel()` ย้ายไปเป็น SQL ใน `exports/dashboard.py` |
| `renderPaymentChart()` | 7623 | `items` | |
| `renderPaymentProducts()` | 7645 | `items` | แถบซ้อน COD/โอน ต่อสินค้า |
| `renderChannelPie()` | 7742 | `items` | โดนัท + legend |
| `renderChannelHeatmap()` | 7840 | `items` | รวม "ดูเพิ่มเติม" ว่าช่องทางนั้นขายอะไร |
| `renderRepHeatmap()` | 7899 | `owners` + `items` | กดแถว = กรองเฉพาะคนนั้น |
| `renderSetChart()` / `runSetSearch()` | 8012 / 8035 | `sets` | |
| `renderTeamBar()` + แถบซ้าย | 8457 | `daily_team` | การ์ดทีม + ตัวเลือกช่วงเวลา |
| `renderPageConversion()` | 8180 | `conversion` | |
| `buildPeriod()` รายวัน/สัปดาห์/เดือน/ช่วงเอง | 6761 | คำนวณใหม่ฝั่งหน้าเว็บ | สูตรเดียวกัน |

### การ์ดที่ **ไม่ย้าย** (เป็นข้อมูลรายลูกค้า)

`renderOrders()` (ออเดอร์ล่าสุด) · `renderTopSpenderCard()` · `renderCancelCard()` / `renderCancelList()` ·
`renderContactCard()` · `renderCallsheetLine()` · `renderPosCard()` · `renderPointCard()` ·
`renderMcCard()` · `exportSegmentation()` — ยังอยู่ที่ `index.html` เดิม ใช้ในเครื่องเท่านั้น

### จุดที่ต่างจาก `index.html` โดยตั้งใจ

| เรื่อง | `index.html` | `dashboard.html` | เหตุผล |
|---|---|---|---|
| ตัวเลือก "เลือกช่วงเอง" | ปฏิทินสองเดือนแบบกดเลือกวันเริ่ม–วันจบ | ช่องวันที่สองช่อง + ปุ่มลัด (7/30/90 วัน · ต้นปี · ทั้งหมด) | ได้ผลเหมือนกัน โค้ดสั้นกว่ามาก |
| ช่วงที่เลือกอยู่ ตอนสลับตัวกรองทีม | อิง "ลำดับที่" — เดือนเด้งได้เมื่อรายการสั้นลง | อิง "คีย์ของช่วง" — อยู่เดือนเดิมเสมอ | แก้พฤติกรรมที่สับสน |
| บรรทัด `ส่วนต่างระดับบิล` | ไม่มีในข้อมูล | มี (`order_items` เก็บไว้ให้ยอดรายสินค้ารวมเท่ายอดออเดอร์) | ไม่ขึ้นกราฟ แต่โชว์ท้ายตารางพร้อมป้าย "ปรับยอด" |
| ช่องทางของออเดอร์ Pancake ที่ไม่มี Lead Intro | `ไม่ระบุช่องทาง` | เดาจากผู้ดูแล (`owner_default_channel`) | ทีมยืนยันกติกาไว้แล้ว |
| ค่าทีมที่สะกดต่างกัน (`Admin` / `admin`) | ไม่มีปัญหา เพราะคิดใหม่ทุกครั้ง | รวมให้เป็นคีย์เดียวตอน export (`team_sql()`) | ไม่งั้นการ์ดทีมแตกเป็นสองใบ |

### ช่องทางบนการ์ด = `xl_bucket` ก่อน แล้วค่อย `channel`

`index.html` บรรทัด 6851 ใช้ `r.__xlBucket || channelLabel(r)` — `__xlBucket` คือช่องทางที่ถูก
"จัดใหม่" ตอนกระทบยอดกับรายงานขาย (§8.1) มีค่าเฉพาะเดือนที่กระทบยอดแล้ว
`exports/dashboard.py` จึงใช้ `COALESCE(xl_bucket, channel)` ให้ตรงกัน
ถ้าใช้ `channel` ดิบอย่างเดียว ยอดของเดือนที่กระทบแล้วจะไปกองที่ `ไม่ระบุช่องทาง` แทนที่จะเป็น `Facebook`

### รูปแบบไฟล์ `.json`

ไฟล์ที่ยาวเกิน `TABLE_MIN_ROWS` (400 แถว) เก็บแบบตาราง แทนที่จะเขียนชื่อคอลัมน์ซ้ำทุกแถว

```json
{"format":"table",
 "cols":["date","owner","team","value","count"],
 "dict":{"owner":["TRADER","กานต์ปริยา ช่างเกิด"],"team":["admin","crm"]},
 "rows":[["2026-09-10",1,0,1290.0,33]]}
```

ค่าของคอลัมน์ที่อยู่ใน `dict` คือ "ลำดับที่" ของค่าจริง — ฝั่งหน้าเว็บมี `expand()` คลี่กลับให้
ข้อมูลเท่าเดิมทุกช่อง เล็กลงราว 4 เท่า (`sets.json` 373 KB → 92 KB)

### ผลการเทียบตัวเลขกับ `index.html` (12 ก.ย. 2026)

วิธีเทียบ: เปิด `index.html` ตัวจริงในเบราว์เซอร์ ปล่อยให้มันคำนวณ `DATA` จาก `EMBEDDED_DATA`
ของมันเอง แล้วดึงผลออกมาเทียบกับไฟล์ `.json` ทีละหัวข้อ

ข้อมูลสองฝั่งไม่เท่ากัน (`index.html` มี 312 วัน · 19,366 ใบ / ฐานข้อมูลมี 303 วัน · 19,801 ใบ)
จึงเทียบเฉพาะ **142 วันที่ทั้งสองฝั่งมีจำนวนออเดอร์เท่ากัน** = วันที่ถือข้อมูลชุดเดียวกัน

| หัวข้อ | ผล |
|---|---|
| ยอดขายรายวัน · จำนวนออเดอร์ · จำนวนชิ้น | **ตรงทุกวัน 142/142** |
| จำนวนชิ้นรายสินค้า (ยอดดิบก่อนกระทบยอด) | **ตรงเป๊ะทุกสินค้า** |
| ยอดดิบรายสินค้า | ต่าง 224 บาท จาก 5.97 ล้าน (0.004%) → §8.15.1 |
| นับจำนวนเซ็ต (วันที่ไม่ได้กระทบยอด) | **ตรงทั้งหมด** |
| ยอด **หลัง**กระทบยอด แยกตามสินค้า/ช่องทาง/วิธีจ่าย | ยอดรวมต่อวันตรงเป๊ะ แต่การกระจายลงแต่ละใบต่างกัน → §8.15.2 |
| ม.ค.–มิ.ย. 2569 เทียบรายงานขายจริง | pipeline ต่าง ≤ 0.41 บาท/เดือน · `index.html` ต่าง 0.00 |

#### 8.15.1 ต่าง 224 บาท — อยู่ที่ 2 วัน จาก loader ของ Google Sheet เก่า

| วัน | ออเดอร์ | เรา | index.html |
|---|---|---:|---:|
| 2026-06-16 | `OLD-20260616-653` "Hylme กระปุก" | 0.00 | 30.00 |
| 2026-06-21 | Olive Oil 9 ใบ | 7,020.00 | 7,220.01 |

เป็นเรื่องของ `loaders/oldsheet.py` ไม่ใช่ของแดชบอร์ด — ยังไม่ได้แก้ในรอบนี้

#### 8.15.2 ทำไมยอดหลังกระทบยอดถึงกระจายไม่เหมือนกัน

ขั้นกระทบยอด (§8.1 ขั้น 3.1) มีขั้นตอน "ออเดอร์ที่อ่านช่องทางไม่ออก → โยนเข้าช่องทางที่ยอดยังขาดมากที่สุด"
เป็นการจัดแบบโลภ (greedy) ผลลัพธ์ขึ้นกับลำดับที่วนออเดอร์ ซึ่งสองระบบไม่เหมือนกัน
→ **ยอดรวมของวันเท่ากันเป๊ะเสมอ** (เพราะถูกตรึงไว้กับรายงานขาย)
แต่ออเดอร์ใบไหนได้ ratio เท่าไรต่างกัน ยอดจึงขยับข้ามสินค้า/ช่องทาง/วิธีจ่าย โดยรวมแล้วหักล้างกันเป็นศูนย์

พิสูจน์: เทียบด้วย **ยอดดิบก่อนกระทบยอด** — จำนวนชิ้นตรงเป๊ะทุกสินค้า
ยอดดิบตรงกันหมดยกเว้น 2 วันใน §8.15.1 แปลว่าสูตรเหมือนกัน ต่างแค่ผลของการจัดแบบโลภ

#### 8.15.3 ฐานข้อมูลยังขาดข้อมูล 10 วัน

`index.html` มีออเดอร์ของ **29 ส.ค. – 7 ก.ย. 2569** รวม 1,052 ใบ 814,370 บาท ที่ยังไม่ได้โหลดเข้าฐานข้อมูล
(ฐานมีแค่ 8 และ 10 ก.ย.) — ต้องเอาไฟล์ต้นทางของช่วงนั้นใส่ `data/raw/` แล้วรัน `daily_pipeline.py`

---

## 9 · วิธีพิสูจน์ว่าตัวเลขไม่เปลี่ยน

รันกับ **สำเนา** ฐานข้อมูลเสมอ (`set HYLME_DB=%USERPROFILE%\hylme_test.db`)

| ตรวจอะไร | เทียบอะไรกับอะไร | เกณฑ์ผ่าน |
|---|---|---|
| จำนวนออเดอร์รายเดือน | `data/output/dashboard/monthly.json` ↔ หน้าสรุปรายเดือนของ `index.html` | เท่ากันทุกเดือน |
| ยอดขายรายเดือน | เหมือนข้างบน | ต่างได้เฉพาะส่วนที่อธิบายได้ด้วย §8.1 |
| ยอดรายวัน × ช่องทาง | `daily_channel.json` ↔ ปุ่ม "ดาวน์โหลด ยอดขายรายวัน × ช่องทาง" | เท่ากัน |
| จำนวนชิ้นรายสินค้า | `products.json` ↔ การ์ด "สินค้าที่ขาย (แยกตามชนิด)" | เท่ากันเป๊ะ (ไม่ขึ้นกับ §8.1) |
| ออเดอร์ยกเลิก | `reconciliation.json` ↔ `CANCEL_APPLIED` (matched / unmatched / ambiguous) | จำนวนแต่ละกลุ่มเท่ากัน |
| ไฟล์ MyCloud | ไฟล์จาก pipeline ↔ ไฟล์จากปุ่มเดิม วันเดียวกัน | diff ทีละเซลล์ = 0 |
| Conversion Rate | `conversion.json` ↔ การ์ด Conversion Rate | เท่ากันทุกเพจ |
| Idempotency | รันซ้ำไฟล์เดิม | ทุกตารางและ hash ไม่เปลี่ยน — **ผ่านแล้ว** |
| หน้าแดชบอร์ดทั้งหน้า | `dashboard.html` ↔ `index.html` ทีละการ์ด | **ผ่านแล้ว** — ดู §8.15 |
| ข้อมูลลูกค้าหลุดไหม | `python pii_scan.py` | ต้องขึ้น `เผยแพร่ได้` — **ผ่านแล้ว** |
